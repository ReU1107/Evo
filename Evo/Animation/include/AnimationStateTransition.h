#pragma once

namespace Alpha
{
	// アニメーションステート遷移
	class AnimationStateTransition
	{
	public:
		// 遷移前,遷移後のステートを受け取る

	private:

	};
	/*	class AnimationStateTransition final : public AnimationStateBase
	{
	private:
		BlendTree* m_blend;			// ブレンドツリーを使用したアニメーション遷移
		AnimationCurve* m_curve;	// ウエイト用アニメーションカーブ
		//

		// 遷移後に次のステート(クリップ)に対して時間を渡す必要がある

	public:
		// 何秒かけて遷移するか
		void SetT(const float time);

		// 遷移条件
		void Set();

	};
	*/

}